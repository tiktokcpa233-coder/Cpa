<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Conectare Imediată - Verificare Confidențialitate</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&display=swap" rel="stylesheet" />

    <style>
        /* Global Styles and Colors (Clean & Professional) */
        body { font-family: 'Inter', sans-serif; margin: 0; padding: 0; background-color: #eef2f7; color: #1f2937; line-height: 1.6; }
        .container { width: 95%; max-width: 600px; margin: 50px auto; padding: 30px; background-color: #ffffff; border-radius: 12px; box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1); text-align: center; }
        
        /* Color Palette */
        :root {
            --primary-color: #FF576E; /* Bright Red/Pink - Attractive */
            --secondary-color: #3b82f6; /* Blue for Security */
            --text-dark: #1f2937;
            --text-light: #4b5563;
        }

        /* Header and Main Message */
        .status-header { display: flex; align-items: center; justify-content: center; margin-bottom: 20px; }
        .icon { font-size: 2.5em; color: var(--secondary-color); margin-right: 10px; }
        .status-header h1 { font-size: 1.8em; font-weight: 900; color: var(--text-dark); margin: 0; }
        .sub-message { font-size: 1.1em; color: var(--text-light); margin-bottom: 30px; }

        /* Security Steps (The Core Message) */
        .step-card { background-color: #f9fafb; border-left: 4px solid var(--primary-color); padding: 15px; margin-bottom: 20px; border-radius: 8px; text-align: left; }
        .step-card h3 { color: var(--primary-color); margin-top: 0; font-size: 1.2em; font-weight: 700; }
        .step-card p { margin: 5px 0 0; color: var(--text-dark); }
        .highlight { color: var(--primary-color); font-weight: 700; }
        
        /* Call to Action Button */
        .cta-button { 
            display: block; 
            width: 100%; 
            padding: 15px 20px; 
            margin-top: 30px;
            font-size: 1.3em; 
            font-weight: 900; 
            color: #fff; 
            background-color: var(--primary-color); 
            border: none; 
            border-radius: 8px; 
            cursor: pointer; 
            transition: background-color 0.3s ease, transform 0.1s ease;
            box-shadow: 0 4px 10px rgba(255, 87, 110, 0.4);
            text-decoration: none;
            text-align: center;
        }
        .cta-button:hover { background-color: #d8455a; transform: translateY(-1px); }

        /* Footer/Legal */
        .footer-note { margin-top: 25px; font-size: 0.8em; color: #9ca3af; }

        /* Small Screen Adjustments */
        @media (max-width: 600px) {
            .container { margin: 20px auto; padding: 20px; }
            .status-header h1 { font-size: 1.5em; }
            .cta-button { font-size: 1.1em; padding: 12px 15px; }
        }
    </style>
</head>
<body>

    <div class="container">
        
        <div class="status-header">
            <span class="icon">🔒</span>
            <h1>Acces Blocat! Necesită Verificare Umană.</h1>
        </div>

        <p class="sub-message">
            Pentru a preveni spam-ul și a proteja calitatea discuțiilor, trebuie să finalizezi rapid un <span class="highlight">test de securitate unic</span> înainte de a fi conectat cu partenerii locali.
        </p>

        <div class="step-card">
            <h3>Pasul 1: Confirmă Interesul Tău</h3>
            <p>Ești la un pas de a vedea profiluri complete și de a începe conversația. Nu mai sunt formulare lungi de completat.</p>
        </div>

        <div class="step-card">
            <h3>Pasul 2: Finalizează Verificarea</h3>
            <p>Vei fi redirecționat către partenerul nostru de securitate. <span class="highlight">Alege un singur test (ofertă CPA)</span> de finalizat pentru a dovedi că ești o persoană reală.</p>
        </div>

        <div class="step-card">
            <h3>Pasul 3: Conectare Imediată</h3>
            <p>După verificare, vei primi acces instantaneu și nelimitat la baza de date completă a femeilor din zona ta.</p>
        </div>

        <a href="https://ridefiles.net/1860478" id="ctaLink" class="cta-button">
            Începe Testul de Securitate ACUM (Durată: 30 de Secunde)
        </a>

        <p class="footer-note">
            Verificarea este necesară o singură dată. Confidențialitatea garantată.
        </p>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const ctaLink = document.getElementById('ctaLink');
            const originalHref = ctaLink.href;
            
            const urlParams = new URLSearchParams(window.location.search);
            const trackingParams = new URLSearchParams();

            // Parameters we want to collect and pass to the CPA network
            const paramsToCollect = ['subid', 'clickid', 'source', 'utm_source', 'param1', 'param2']; 

            paramsToCollect.forEach(param => {
                if (urlParams.has(param)) {
                    trackingParams.set(param, urlParams.get(param));
                }
            });

            // Reconstruct the final URL for the CTA button with all tracking parameters
            if (trackingParams.toString()) {
                const finalUrl = originalHref + '?' + trackingParams.toString();
                ctaLink.href = finalUrl;
            }
        });
    </script>
</body>
</html>
